what are design tokens:
named entities(any name given to values) which stores visual design attributes(spacing, shadows)

benefits:
define them once and reference them throughout your system.

Tokens folder:
this folder contains themes where each theme have two modes light and dark.
